<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cigare Casa - L'Excellence du Cigare</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Georgia', serif;
            background: linear-gradient(135deg, #0a0604 0%, #1a0f0a 50%, #2a1810 100%);
            color: #e8d4b0;
            overflow-x: hidden;
        }

        .smoke-effect {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(ellipse at 20% 30%, rgba(139, 105, 20, 0.1) 0%, transparent 50%),
                radial-gradient(ellipse at 80% 70%, rgba(139, 105, 20, 0.08) 0%, transparent 50%);
            pointer-events: none;
            z-index: 0;
        }

        header {
            background: linear-gradient(to bottom, rgba(42, 24, 16, 0.95), rgba(26, 15, 10, 0.95));
            border-bottom: 3px solid #8b6914;
            padding: 30px 20px;
            text-align: center;
            position: relative;
            z-index: 10;
            box-shadow: 0 5px 30px rgba(0, 0, 0, 0.8);
        }

        .logo {
            display: inline-block;
            margin-bottom: 15px;
        }

        .cigar-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto;
            background: radial-gradient(circle, #8b6914, #6b5310);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 45px;
            box-shadow: 0 8px 25px rgba(139, 105, 20, 0.5);
            animation: glow 3s ease-in-out infinite;
        }

        @keyframes glow {
            0%, 100% { box-shadow: 0 8px 25px rgba(139, 105, 20, 0.5); }
            50% { box-shadow: 0 8px 35px rgba(139, 105, 20, 0.8); }
        }

        h1 {
            font-size: 3.5em;
            color: #d4af37;
            text-transform: uppercase;
            letter-spacing: 5px;
            text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.9);
            margin: 10px 0;
            font-style: italic;
        }

        .tagline {
            font-size: 1.3em;
            color: #c9a961;
            font-style: italic;
            letter-spacing: 3px;
            margin-top: 10px;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 40px 20px;
            position: relative;
            z-index: 1;
        }

        .intro {
            text-align: center;
            font-size: 1.3em;
            color: #d4c5a9;
            margin-bottom: 50px;
            line-height: 1.8;
            padding: 30px;
            background: rgba(42, 24, 16, 0.4);
            border-radius: 10px;
            border: 2px solid rgba(139, 105, 20, 0.3);
        }

        .section-title {
            text-align: center;
            font-size: 2.5em;
            color: #d4af37;
            text-transform: uppercase;
            letter-spacing: 4px;
            margin: 60px 0 40px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
            position: relative;
        }

        .section-title::after {
            content: '';
            display: block;
            width: 200px;
            height: 3px;
            background: linear-gradient(to right, transparent, #8b6914, transparent);
            margin: 15px auto 0;
        }

        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 30px;
            margin-bottom: 60px;
        }

        .menu-card {
            background: linear-gradient(135deg, rgba(42, 24, 16, 0.8), rgba(26, 15, 10, 0.8));
            border: 3px solid #8b6914;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .menu-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 50px rgba(139, 105, 20, 0.4);
        }

        .menu-card h3 {
            color: #d4af37;
            font-size: 1.8em;
            text-align: center;
            margin-bottom: 25px;
            text-transform: uppercase;
            letter-spacing: 2px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .menu-card h3 .icon {
            font-size: 1.2em;
        }

        .menu-list {
            list-style: none;
        }

        .menu-item {
            padding: 12px 0;
            border-bottom: 1px solid rgba(139, 105, 20, 0.3);
            font-size: 1.1em;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .menu-item:last-child {
            border-bottom: none;
        }

        .menu-item::before {
            content: '◆';
            color: #d4af37;
            font-size: 0.8em;
            flex-shrink: 0;
        }

        .benefits {
            background: linear-gradient(135deg, rgba(42, 24, 16, 0.6), rgba(26, 15, 10, 0.6));
            border: 3px solid #8b6914;
            border-radius: 15px;
            padding: 40px;
            text-align: center;
            margin: 60px 0;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
        }

        .benefits h2 {
            color: #d4af37;
            font-size: 2em;
            margin-bottom: 30px;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .benefits-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }

        .benefit-card {
            background: linear-gradient(135deg, #8b6914, #6b5310);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.5);
            transition: transform 0.3s ease;
        }

        .benefit-card:hover {
            transform: scale(1.05);
        }

        .benefit-card .icon {
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        .benefit-card h4 {
            color: #fff;
            font-size: 1.1em;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .contact-section {
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.15), rgba(139, 105, 20, 0.15));
            border: 4px solid #d4af37;
            border-radius: 15px;
            padding: 50px;
            text-align: center;
            margin: 60px 0;
            box-shadow: 0 10px 40px rgba(212, 175, 55, 0.3);
        }

        .contact-section h2 {
            color: #d4af37;
            font-size: 2.5em;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .contact-info {
            font-size: 1.3em;
            margin: 20px 0;
            color: #e8d4b0;
        }

        .contact-highlight {
            color: #d4af37;
            font-weight: bold;
            font-size: 1.5em;
            display: inline-block;
            margin: 10px;
            padding: 10px 20px;
            background: rgba(139, 105, 20, 0.2);
            border-radius: 5px;
        }

        .btn {
            background: linear-gradient(135deg, #d4af37, #8b6914);
            color: #1a0f0a;
            border: none;
            padding: 15px 40px;
            font-size: 1.2em;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 0 5px 20px rgba(139, 105, 20, 0.5);
            transition: all 0.3s ease;
            margin: 10px;
            font-family: 'Georgia', serif;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(212, 175, 55, 0.7);
            background: linear-gradient(135deg, #e8c456, #9b7920);
        }

        .btn:active {
            transform: translateY(0);
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            animation: fadeIn 0.3s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .modal-content {
            background: linear-gradient(135deg, #2a1810, #1a0f0a);
            border: 4px solid #d4af37;
            border-radius: 15px;
            margin: 5% auto;
            padding: 40px;
            max-width: 600px;
            box-shadow: 0 20px 60px rgba(212, 175, 55, 0.5);
            animation: slideDown 0.4s ease;
        }

        @keyframes slideDown {
            from { 
                transform: translateY(-50px);
                opacity: 0;
            }
            to { 
                transform: translateY(0);
                opacity: 1;
            }
        }

        .modal-header {
            color: #d4af37;
            font-size: 2em;
            text-align: center;
            margin-bottom: 30px;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .close {
            color: #d4af37;
            float: right;
            font-size: 40px;
            font-weight: bold;
            cursor: pointer;
            line-height: 20px;
            transition: color 0.3s ease;
        }

        .close:hover {
            color: #e8c456;
        }

        .form-group {
            margin: 20px 0;
        }

        .form-group label {
            display: block;
            color: #c9a961;
            font-size: 1.1em;
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 12px;
            background: rgba(42, 24, 16, 0.6);
            border: 2px solid #8b6914;
            border-radius: 5px;
            color: #e8d4b0;
            font-size: 1em;
            font-family: 'Georgia', serif;
        }

        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            outline: none;
            border-color: #d4af37;
            box-shadow: 0 0 10px rgba(212, 175, 55, 0.3);
        }

        .success-message {
            display: none;
            background: rgba(80, 200, 120, 0.2);
            border: 2px solid #50c878;
            color: #50c878;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            font-size: 1.2em;
            margin-top: 20px;
        }

        .copy-btn {
            background: linear-gradient(135deg, #8b6914, #6b5310);
            color: #fff;
            border: none;
            padding: 8px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 0.9em;
            margin-left: 10px;
            transition: all 0.3s ease;
        }

        .copy-btn:hover {
            background: linear-gradient(135deg, #9b7920, #7b6320);
            transform: scale(1.05);
        }

        footer {
            background: linear-gradient(to top, rgba(42, 24, 16, 0.95), rgba(26, 15, 10, 0.95));
            border-top: 3px solid #8b6914;
            padding: 30px;
            text-align: center;
            color: #a89060;
            font-style: italic;
            margin-top: 80px;
        }

        footer p {
            font-size: 1.2em;
            margin: 10px 0;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2em;
            }

            .menu-grid {
                grid-template-columns: 1fr;
            }

            .section-title {
                font-size: 1.8em;
            }

            .contact-section {
                padding: 30px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="smoke-effect"></div>

    <header>
        <div class="logo">
            <div class="cigar-icon">🚬</div>
        </div>
        <h1>Cigare Casa</h1>
        <p class="tagline">L'Excellence du Cigare Premium</p>
    </header>

    <div class="container">
        <div class="intro">
            Bienvenue chez Cigare Casa, votre destination exclusive pour les meilleurs cigares, cigarettes et spécialités gastronomiques. Découvrez notre sélection premium et laissez-vous transporter dans un univers de raffinement et de saveurs exceptionnelles.
        </div>

        <h2 class="section-title">Notre Carte</h2>

        <div class="menu-grid">
            <div class="menu-card">
                <h3><span class="icon">🍹</span> Nos Boissons</h3>
                <ul class="menu-list">
                    <li class="menu-item">Cedrata</li>
                    <li class="menu-item">Fruity Purple</li>
                    <li class="menu-item">Red Velvet</li>
                    <li class="menu-item">Blue Star</li>
                    <li class="menu-item">Misty Green</li>
                </ul>
            </div>

            <div class="menu-card">
                <h3><span class="icon">🚬</span> Nos Cigares</h3>
                <ul class="menu-list">
                    <li class="menu-item">Partagas</li>
                    <li class="menu-item">Veridians</li>
                    <li class="menu-item">Robusto Cubain</li>
                </ul>
            </div>

            <div class="menu-card">
                <h3><span class="icon">🚭</span> Nos Cigarettes</h3>
                <ul class="menu-list">
                    <li class="menu-item">Debonaires</li>
                    <li class="menu-item">Redwood</li>
                </ul>
            </div>

            <div class="menu-card">
                <h3><span class="icon">🍪</span> Nos Biscuits</h3>
                <ul class="menu-list">
                    <li class="menu-item">Ébène Gourmande</li>
                    <li class="menu-item">Douceur Boréal</li>
                </ul>
            </div>

            <div class="menu-card">
                <h3><span class="icon">🍖</span> Nos Plats</h3>
                <ul class="menu-list">
                    <li class="menu-item">Menu Bœuf</li>
                </ul>
            </div>
        </div>

        <div class="benefits">
            <h2>Pourquoi Choisir Cigare Casa ?</h2>
            <div class="benefits-grid">
                <div class="benefit-card">
                    <div class="icon">⭐</div>
                    <h4>Qualité Premium</h4>
                </div>
                <div class="benefit-card">
                    <div class="icon">🎯</div>
                    <h4>Sélection Exclusive</h4>
                </div>
                <div class="benefit-card">
                    <div class="icon">👔</div>
                    <h4>Service Expert</h4>
                </div>
                <div class="benefit-card">
                    <div class="icon">💎</div>
                    <h4>Produits Uniques</h4>
                </div>
                <div class="benefit-card">
                    <div class="icon">💰</div>
                    <h4>Prix Compétitifs</h4>
                </div>
            </div>
        </div>

        <div class="contact-section">
            <h2>Commandez Dès Maintenant</h2>
            <p class="contact-info">Rejoignez-nous sur la plateforme</p>
            <div class="contact-highlight">EyeFind</div>
            <p class="contact-info">
                📞 Téléphone : <span class="contact-highlight">555 929 25</span>
                <button class="copy-btn" onclick="copyPhone()">📋 Copier</button>
            </p>
            <button class="btn" onclick="openModal()">📝 Commander en ligne</button>
        </div>
    </div>

    <!-- Modal de commande -->
    <div id="orderModal" class="modal">
        <div class="modal-content">
            <span class="close" onclick="closeModal()">&times;</span>
            <h2 class="modal-header">Formulaire de Commande</h2>
            
            <form id="orderForm" onsubmit="submitOrder(event)">
                <div class="form-group">
                    <label for="name">Votre Nom :</label>
                    <input type="text" id="name" name="name" required placeholder="Ex: John Doe">
                </div>

                <div class="form-group">
                    <label for="phone">Téléphone :</label>
                    <input type="tel" id="phone" name="phone" required placeholder="Ex: 555 123 45">
                </div>

                <div class="form-group">
                    <label for="category">Catégorie :</label>
                    <select id="category" name="category" required>
                        <option value="">-- Sélectionnez --</option>
                        <option value="boissons">🍹 Boissons</option>
                        <option value="cigares">🚬 Cigares</option>
                        <option value="cigarettes">🚭 Cigarettes</option>
                        <option value="biscuits">🍪 Biscuits</option>
                        <option value="plats">🍖 Plats</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="message">Détails de votre commande :</label>
                    <textarea id="message" name="message" rows="4" required placeholder="Précisez les produits et quantités..."></textarea>
                </div>

                <button type="submit" class="btn">✅ Valider la commande</button>
            </form>

            <div id="successMessage" class="success-message">
                ✅ Commande envoyée avec succès ! Nous vous contacterons bientôt.
            </div>
        </div>
    </div>

    <script>
        function openModal() {
            document.getElementById('orderModal').style.display = 'block';
            document.body.style.overflow = 'hidden';
        }

        function closeModal() {
            document.getElementById('orderModal').style.display = 'none';
            document.body.style.overflow = 'auto';
            document.getElementById('successMessage').style.display = 'none';
            document.getElementById('orderForm').reset();
        }

        function submitOrder(event) {
            event.preventDefault();
            
            // Simulation d'envoi de commande
            const form = document.getElementById('orderForm');
            const successMsg = document.getElementById('successMessage');
            
            form.style.display = 'none';
            successMsg.style.display = 'block';

            // Réinitialiser après 3 secondes
            setTimeout(() => {
                closeModal();
                form.style.display = 'block';
            }, 3000);
        }

        function copyPhone() {
            const phone = '555 929 25';
            navigator.clipboard.writeText(phone).then(() => {
                const btn = event.target;
                const originalText = btn.textContent;
                btn.textContent = '✅ Copié !';
                btn.style.background = 'linear-gradient(135deg, #50c878, #40a868)';
                
                setTimeout(() => {
                    btn.textContent = originalText;
                    btn.style.background = '';
                }, 2000);
            });
        }

        // Fermer modal en cliquant en dehors
        window.onclick = function(event) {
            const modal = document.getElementById('orderModal');
            if (event.target == modal) {
                closeModal();
            }
        }
    </script>

    <footer>
        <p>"La passion du cigare, l'excellence du service"</p>
        <p>© 2024 Cigare Casa - Tous droits réservés</p>
    </footer>
</body>
</html>
